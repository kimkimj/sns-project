spring:
    mvc:
        pathmatch:
            matching-strategy: ant_path_matcher
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: localhost
        username: root
        password: password

jwt:
    token:
        secret: secretKey



